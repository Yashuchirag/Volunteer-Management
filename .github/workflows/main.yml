name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.13.15 # This is the action
        with:
          heroku_api_key: ${{secrets.HRKU-f159c263-d0f4-49a9-abda-7135b1370fa1}}
          heroku_app_name: "blooming-reef-79073" #Must be unique in Heroku
          heroku_email: "chiragchandrashekar@gmail.com"
          buildpack: "https://github.com/heroku/heroku-buildpack-python" # Example for Python buildpack
          branch: "main"